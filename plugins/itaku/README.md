# Itaku Plugin

Downloads images from [Itaku.ee](https://itaku.ee).

## Supported URLs

- `https://itaku.ee/images/{id}` — single image
- `https://itaku.ee/profile/{username}/gallery` — user gallery

## Notes

Uses the public Itaku API (`/api/galleries/images/`) for metadata and download URLs.
